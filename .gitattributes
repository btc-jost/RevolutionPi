# Normalize line endings: text files stored as LF in the repo.
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.pdn binary
*.ico binary
*.zip binary
*.exe binary
*.dll binary
*.snk binary
